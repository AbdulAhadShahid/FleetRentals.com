@model Fleet_Rentals.Models.SignUp
@{
    Layout = "~/Views/Shared/Layout.cshtml";
}

@section customer
{
    <title>Customer Details</title>

    <link rel="stylesheet" href="~/content/customer.css" />

    @*<script>

        $(document).ready(function ()
        {
            $('#error').hide();

            $('#signin').click(function ()
            {
                var userName = $('#Username').val(); 

                var pass = $('#Password').val(); 


                if (userName == null || pass == null)
                {}
                else
                {
                    $.getJSON("/RecordView/CheckLogin?Username=" + userName,"&Password=" +pass, function (data)
                    {
                        if (data)
                        {
                            $('#error').show();

                        }
                        else
                        {
                            $('#error').show();
                        }
                    });
                }

            });

        });
    </script>*@
}
  

<div id="mid">
<p id="head" style="font-size:36px"> Customer Sign in <hr style="color:#f5f5f5"/> </p>

<p style="font-size:25px; margin-top:35px">Customers<br /><br />

<font id="p1">
The online customer area is restricted to Westline Transport customers with a valid login and password. If
  you have not been supplied with sign in details or do not have your password, please contact us. Our contact details can be found on the
 @Html.ActionLink("contact us page", "contactus", "Contactus")
  </p>


@{Html.BeginForm("CheckLogin", "Customer", FormMethod.Post);}

@Html.Label("Sign in", new {style="font-size:20px; margin-left:92px; margin-right:40px"}) or @Html.ActionLink("create an account", "signup", "Signup")<br />

@Html.TextBoxFor(model => model.Username, new {placeholder="Username", @Autocomplete= "off"})
<br/><br/>

@Html.TextBoxFor(model => model.Password, new {type="password", placeholder="Password", @Autocomplete= "off"})
<br/><br/>

@*<a href="/Signin/signin">*@
 <input id="signin" type="submit" value="Sign In" />@*</a>*@
 <br>
 
 
 @{Html.EndForm();}
 <br />
 
@Html.ValidationMessage("error")
 
 </div>
 

